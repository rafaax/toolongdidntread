import nltk

nltk.download('punkt', quiet=True) # Tokenizador de palavras e sentenças
nltk.download('stopwords', quiet=True) # Lista de stopwords (palavras comuns) em vários idiomas
nltk.download('rslp', quiet=True)  # Stemmer para o português (RSLP)